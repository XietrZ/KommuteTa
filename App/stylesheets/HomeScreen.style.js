import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    // backgroundColor: "cyan",
    flex: 1,
  },

  belowContainerWrapper: {
    flex: 1,
    // backgroundColor: "green",
  },
});

export default styles;
